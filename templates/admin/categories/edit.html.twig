{% extends 'base.html.twig' %}

{% block body %}
{% apply inline_css(source("styles/login.css")) %}
<div class="box">
{% if isAdd %}
        <h1>Ajout d'une catégorie</h1>
    {% else %}
        <h1>Modification d'une categorie {{ category.name }}</h1>
    {% endif %}
    {{ form_start(form) }}
    {{ form_widget(form.name, {'attr': {'class': 'email', 'placeholder': 'Nom'}}) }}
    {{ form_widget(form.type, {'attr': {'class': 'email', 'placeholder': 'Nom'}}) }}
    <button class="btn" type="submit">Valider!</button>
    {{ form_end(form) }}
    </div> <!-- End Box -->
    {% endapply %}
{% endblock %}
